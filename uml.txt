classDiagram

    class Calculate{
        - sessions[]

        + load()
        + save()

        + editWeek()

        + calculateWeeks(Week, Week)
        + calculateHills(Hill, Hill)
    }
    class Week{
        - Checaugau
        - Pioneer
        - Wildernes

        + getChecaugau()
        + getPioneer()
        + getWilderness()
        
        + editChecaugau()
        + editPioneer()
        + editWilderness()
    }
    class Hill{
        - sites[]
        
        + getSites()
        + getTotalCots()
        + getTotalTents()

        + editSites()
    }

    <<abstract>> Hill

    class Site {
        - units[]

        + getUnits()
        
        + getCots(): int
        + getTents(): int

        + addUnit()
        + deleteUnit()
    }

    class Unit{
        - youthM: int
        - youthF: int
        - adultM: int
        - youthF: int
        - extraCots: int
        - extraTents: int

        + getCots(): int
        + getTents(): int
        
        + setExtraCots(int)
        + setExtraTents(int)
        
        + getYouth(): int
        + setYouth(int)
        + getAdults(): int
        + setAdults(int)
    }

    Site o-- Unit
    Hill o-- Site
    Week o-- Hill
    Calculate o-- Week
    
